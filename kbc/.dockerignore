# Keep test files out of the compile-box image (the Dockerfile COPYs
# platform/pod/*.py with a glob; tests are not needed at runtime).
platform/pod/test_*.py
platform/pod/pi-runner/node_modules/
platform/pod/pi-runner/dist/
