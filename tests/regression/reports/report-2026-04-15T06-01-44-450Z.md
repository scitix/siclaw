# 回归测试报告 — 2026-04-15T06:04:30.590Z

**整体结果**: ❌ FAIL

| 字段 | 值 |
|---|---|
| Run ID | `rmnzn7lqe` |
| 开始时间 | 2026-04-15T06:01:45.926Z |
| 结束时间 | 2026-04-15T06:04:30.590Z |
| 总耗时 | 2m44s |
| 总计 | 4 |
| 通过 | 2 |
| 失败 | 0 |
| 跳过 | 1 |
| 错误 | 1 |

## 汇总

| Case | 类型 | 命令分 | 结论分 | 阈值 | 耗时 | 结果 |
|---|---|---|---|---|---|---|
| `oom-basic-001` | reproducible | 4 | 5 | 4/4 | 38.1s | ✅ PASS |
| `probe-unreachable-002` | reproducible | 4 | 5 | 3/4 | 1m22s | ✅ PASS |
| `probe-timeout-003` | reproducible | - | - | 3/4 | 44.0s | ⚠️ ERROR |
| `node-notready-003` | stubbed | - | - | 3/4 | 0ms | ⏭️ SKIP |

## 失败 / 跳过 / 错误 详情

### ⚠️ `probe-timeout-003` — livenessProbe 执行超时 — 应用响应延迟过大导致探针超时

**结果**: ERROR
**原因**: Scoring failed: Expected property name or '}' in JSON at position 1 (line 1 column 2)
**工单难度**: green
**工单**:

> Pod `probe-timeout-003-rmnzn7lqe` 在 namespace `deveval-rmnzn7lqe` 反复被 kubelet 重启,`kubectl describe` 提示 `Liveness probe failed: command ... timed out`,请排查为什么探针超时。

**期望结论**:

```
**根因**: 容器 `app` 的 `livenessProbe.exec` 命令 `sleep 10 && echo ok` 需要至少 10 秒才返回,但 `timeoutSeconds` 只给了 2 秒——每次探针都必然超时。kubelet 连续 `failureThreshold=2` 次判定失败后杀掉容器,`restartPolicy: Always` 导致反复重启。events 中会出现 `Unhealthy: Liveness probe failed: command "..." timed out` 和 `Killing` 事件。

**修复建议**:
1. 优先提高 `timeoutSeconds`(比如从 2 提到 15),给应用足够响应时间;必要时同步拉长 `periodSeconds` 避免堆积;
2. 排查应用自身:响应慢是否因为启动未就绪?如果是冷启动慢,应单独配置 `startupProbe` 保护初始化阶段;
3. 若探针命令本身是 shell 管道,确认每一步都能在 timeout 内完成,避免外部依赖阻塞探针。

---
```

**Agent 实际结论**:

```
 我来帮你排查这个 Liveness probe 超时导致 Pod 反复重启的问题。

首先进行环境预检查，并查看相关诊断技能。 让我按照诊断流程来排查这个 Pod 的 Liveness probe 超时问题。 ## 根因分析

通过分析 Pod 状态和事件，发现问题非常明确：

**Liveness 探针配置不匹配**

```
Liveness: exec [sh -c sleep 10 && echo ok] delay=5s timeout=2s period=5s
```

| 配置项 | 当前值 | 执行需求 |
|--------|--------|----------|
| `timeoutSeconds` | **2s** | 实际执行需要 **10s** |

探针命令包含 `sleep 10`，这意味着整个命令需要 10 秒才能完成，但 kubelet 只等待 2 秒就判定超时，导致探针反复失败，Pod 被重启。

---

## 修复建议

### 方案 1：增加 timeoutSeconds（推荐）

修改 Pod 的 Liveness 探针配置，将 `timeoutSeconds` 增加到 15 秒：

```yaml
livenessProbe:
  exec:
    command:
    - sh
    - -c
    - sleep 10 && echo ok
  initialDelaySeconds: 5
  timeoutSeconds: 15    # 原来是 2，改为 15（大于 sleep 10）
  periodSeconds: 5
```

### 方案 2：缩短探针执行时间

如果 10 秒的 sleep 不是必须的，可以将其缩短：

```yaml
livenessProbe:
  exec:
    command:
    - sh
    - -c
    - sleep 1 && echo ok   # 改为 sleep 1，timeout 2s 即可满足
  timeoutSeconds: 2
```

---

**建议**：优先选择方案 1，确认应用程序确实需要这么长时间来响应健康检查。如果应用实际启动/检查时间很短，则应采用方案 2 优化探针命令。
```

**Agent 执行的命令**:

```
[cluster_info] {}
[credential_list] {}
[read] {"path":"/home/yye/siclaw/.siclaw/skills/user/b1ba878783751e80/resolved/pod-crash-debug/SKILL.md"}
[bash] kubectl get pod probe-timeout-003-rmnzn7lqe -n deveval-rmnzn7lqe -o wide
[bash] kubectl describe pod probe-timeout-003-rmnzn7lqe -n deveval-rmnzn7lqe
[bash] kubectl logs probe-timeout-003-rmnzn7lqe -n deveval-rmnzn7lqe --previous --tail=200 2>/dev/null || kubectl logs probe-timeout-003-rmnzn7lqe -n deveval-rmnzn7lqe --tail=200
```

**注入输出**: `namespace/deveval-rmnzn7lqe unchanged
pod/probe-timeout-003-rmnzn7lqe created`

---

### ⏭️ `node-notready-003` — 节点 NotReady 导致 Pod 长期 Pending

**结果**: SKIP
**原因**: Fixture-replay (stubbed case) not yet implemented. stubReason: 节点级故障无法在共享测试集群安全模拟,线上排查时故障已由 kubelet 自愈,使用历史 fixture 回放

**期望结论**:

```
**根因**: 集群中 `node-3` 的 kubelet 已停止上报状态(`Kubelet stopped posting node status`),节点被控制面标记为 `NotReady`,并自动打上 `node.kubernetes.io/unreachable:NoSchedule/NoExecute` 污点;同时该业务 Pod 配置了 nodeAffinity/selector,只能调度到符合条件的节点上,其余 4 个 Ready 节点不匹配亲和性,因此 `default-scheduler` 报 `FailedScheduling`,Pod 长时间 Pending。真正的调度失败原因是**匹配节点 node-3 不可用**,而不是集群容量不足。

**修复建议**:
1. 优先排查 `node-3` kubelet 与 API Server 的连通性:检查 kubelet 进程、节点网络、`kubelet.service` 日志;
2. 短期缓解:放宽 Pod 的 nodeAffinity/selector,让它能调度到其他 Ready 节点;
3. 长期改进:对关键业务添加 `tolerations` 或扩展可调度节点池,避免单节点故障阻塞调度。
```
```

---

