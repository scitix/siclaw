# 回归测试报告 — 2026-04-15T06:12:50.626Z

**整体结果**: ❌ FAIL

| 字段 | 值 |
|---|---|
| Run ID | `rmnznhpx3` |
| 开始时间 | 2026-04-15T06:09:37.911Z |
| 结束时间 | 2026-04-15T06:12:50.626Z |
| 总耗时 | 3m12s |
| 总计 | 4 |
| 通过 | 2 |
| 失败 | 1 |
| 跳过 | 1 |
| 错误 | 0 |

## 汇总

| Case | 类型 | 命令分 | 结论分 | 阈值 | 耗时 | 结果 |
|---|---|---|---|---|---|---|
| `oom-basic-001` | reproducible | 4 | 5 | 4/4 | 50.6s | ✅ PASS |
| `probe-unreachable-002` | reproducible | 4 | 2 | 3/4 | 1m23s | ❌ FAIL |
| `probe-timeout-003` | reproducible | 5 | 5 | 3/4 | 58.8s | ✅ PASS |
| `node-notready-003` | stubbed | - | - | 3/4 | 0ms | ⏭️ SKIP |

## 失败 / 跳过 / 错误 详情

### ❌ `probe-unreachable-002` — livenessProbe 目标不可达 — 网络连通性/丢包导致容器被反复重启

**结果**: FAIL
**工单难度**: green
**工单**:

> Pod `probe-unreachable-002-rmnznhpx3` 在 namespace `deveval-rmnznhpx3` 反复被 kubelet 重启,`kubectl describe` 能看到 `Liveness probe failed: dial tcp 10.255.255.1:80: i/o timeout` 类事件,请定位根因。

**评分理由**:

> Diagnostic commands were nearly perfect - ran all expected kubectl commands plus useful validation steps. However, the root cause conclusion was fundamentally wrong: the agent mischaracterized the livenessProbe as a 'configuration error' and suggested removing or changing it to localhost, missing that external-probe is a valid pattern for detecting network partitions. The fix recommendations would mask the real infrastructure problem rather than addressing why the target IP is unreachable in the cluster.

**期望结论**:

```
**根因**: 容器 `app` 的 `livenessProbe.tcpSocket` 指向 `10.255.255.1:80`,该 IP 在集群内不可达(黑洞 / 100% 丢包),kubelet 每 5 秒探测一次,连续 2 次超时后判定容器不健康并重启。由于 `restartPolicy: Always`,Pod 反复进入重启循环,events 中会出现 `Unhealthy: Liveness probe failed: dial tcp 10.255.255.1:80: i/o timeout` 和 `Killing` 事件。

**修复建议**:
1. 核对 `livenessProbe` 的目标地址是否正确——业务探针通常应打到 `127.0.0.1`(容器自身端口),不应打向外部 IP;
2. 如果确实需要探测外部依赖,改用 `readinessProbe`(外部依赖异常时只会摘掉流量,不会杀容器);
3. 确认网络策略 / 路由:目标网段是否被 NetworkPolicy、CNI、或上层路由丢弃。

---
```

**Agent 实际结论**:

```
 我来帮你排查这个 Pod 的健康检查失败问题。首先让我了解一下集群环境和获取必要信息。  我注意到 Pod 刚刚创建（9秒前）。健康检查配置指向 `10.255.255.1:80`，而 Pod 的 IP 是 `172.16.243.207`。让我查看历史事件来分析问题：   问题已经很明确了！Pod 的 Liveness Probe 配置错误地指向外部地址 `10.255.255.1:80`，但容器并没有在监听这个端口。让我进一步验证这个问题的根因： ## 诊断结果

### 根因：**Liveness Probe 配置错误**

| 问题项 | 详情 |
|--------|------|
| **健康检查目标** | `tcpSocket: {host: 10.255.255.1, port: 80}` |
| **Pod 实际 IP** | `172.16.243.207` |
| **容器行为** | `while true; do sleep 30; done`（仅 sleep，无服务监听） |

### 问题分析

1. **健康检查目标不可达**：Liveness Probe 配置的 `host: 10.255.255.1` 是一个外部地址，从 Pod 内 ping 测试 100% 丢包（`ping failed, 100% packet loss`）

2. **容器无服务监听**：该 Pod 只是一个 busybox 的无限 sleep 循环，没有任何服务在 80 端口监听

3. **健康检查超时**：由于目标地址不可达，TCP 连接超时（`i/o timeout`），kubelet 判定 Pod 不健康，触发容器重启

### 修复建议

根据你的实际需求选择以下方案之一：

#### 方案 A：移除 Liveness Probe（推荐，如果是空跑容器）
```yaml
# 直接删除 livenessProbe 字段
spec:
  containers:
  - name: app
    image: busybox:1.36
    command: ["sh", "-c", "while true; do sleep 30; done"]
    # 删除以下部分
    # livenessProbe:
    #   tcpSocket:
    #     host: 10.255.255.1
    #     port: 80
```

#### 方案 B：修正为检查 Pod 自身（如果容器有实际服务）
```yaml
# 去掉 host 字段，默认检查 Pod IP
livenessProbe:
  tcpSocket:
    # host: 10.255.255.1  # 删除此行
    port: 8080  # 改为容器实际监听的端口
  initialDelaySeconds: 5
  periodSeconds: 5
```

#### 方案 C：改为 exec 类型的健康检查
```yaml
livenessProbe:
  exec:
    command:
    - sh
    - -c
    - "ps aux | grep -v grep | grep 'sleep'"
  initialDelaySeconds: 5
  periodSeconds: 10
```

### 当前 Pod 状态
- Pod 刚刚重建（14秒前），还未积累足够重启次数
- 但在持续监测中可以看到 `Unhealthy` 警告事件

**建议**：如果这是一个测试/空跑容器，使用 **方案 A** 直接移除健康检查，这是最合理的修复方式。
```

**Agent 执行的命令**:

```
[cluster_info] {}
[credential_list] {}
[read] {"path":"/home/yye/siclaw/.siclaw/skills/user/b1ba878783751e80/resolved/pod-crash-debug/SKILL.md"}
[bash] kubectl get pod probe-unreachable-002-rmnznhpx3 -n deveval-rmnznhpx3 -o wide
[bash] kubectl describe pod probe-unreachable-002-rmnznhpx3 -n deveval-rmnznhpx3
[bash] kubectl get events -n deveval-rmnznhpx3 --field-selector involvedObject.name=probe-unreachable-002-rmnznhpx3 --sort-by=lastTimestamp
[bash] kubectl get pod probe-unreachable-002-rmnznhpx3 -n deveval-rmnznhpx3 -o yaml | grep -A20 "livenessProbe"
[pod_exec] ip addr show
[pod_exec] ping -c 2 -W 2 10.255.255.1
```

**注入输出**: `namespace/deveval-rmnznhpx3 unchanged
pod/probe-unreachable-002-rmnznhpx3 created`

---

### ⏭️ `node-notready-003` — 节点 NotReady 导致 Pod 长期 Pending

**结果**: SKIP
**原因**: Fixture-replay (stubbed case) not yet implemented. stubReason: 节点级故障无法在共享测试集群安全模拟,线上排查时故障已由 kubelet 自愈,使用历史 fixture 回放

**期望结论**:

```
**根因**: 集群中 `node-3` 的 kubelet 已停止上报状态(`Kubelet stopped posting node status`),节点被控制面标记为 `NotReady`,并自动打上 `node.kubernetes.io/unreachable:NoSchedule/NoExecute` 污点;同时该业务 Pod 配置了 nodeAffinity/selector,只能调度到符合条件的节点上,其余 4 个 Ready 节点不匹配亲和性,因此 `default-scheduler` 报 `FailedScheduling`,Pod 长时间 Pending。真正的调度失败原因是**匹配节点 node-3 不可用**,而不是集群容量不足。

**修复建议**:
1. 优先排查 `node-3` kubelet 与 API Server 的连通性:检查 kubelet 进程、节点网络、`kubelet.service` 日志;
2. 短期缓解:放宽 Pod 的 nodeAffinity/selector,让它能调度到其他 Ready 节点;
3. 长期改进:对关键业务添加 `tolerations` 或扩展可调度节点池,避免单节点故障阻塞调度。
```
```

---

