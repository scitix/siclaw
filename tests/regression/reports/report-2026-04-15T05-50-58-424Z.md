# 回归测试报告 — 2026-04-15T05:52:58.204Z

**整体结果**: ❌ FAIL

| 字段 | 值 |
|---|---|
| Run ID | `rmnzmtrfs` |
| 开始时间 | 2026-04-15T05:51:00.136Z |
| 结束时间 | 2026-04-15T05:52:58.204Z |
| 总耗时 | 1m58s |
| 总计 | 3 |
| 通过 | 1 |
| 失败 | 1 |
| 跳过 | 1 |
| 错误 | 0 |

## 汇总

| Case | 类型 | 命令分 | 结论分 | 阈值 | 耗时 | 结果 |
|---|---|---|---|---|---|---|
| `oom-basic-001` | reproducible | 4 | 5 | 4/4 | 42.4s | ✅ PASS |
| `imagepull-private-002` | reproducible | 2 | 1 | 4/4 | 1m15s | ❌ FAIL |
| `node-notready-003` | stubbed | - | - | 3/4 | 0ms | ⏭️ SKIP |

## 失败 / 跳过 / 错误 详情

### ❌ `imagepull-private-002` — 私有镜像仓库无凭据导致 ImagePullBackOff

**结果**: FAIL
**工单难度**: green
**工单**:

> Pod `imagepull-private-002-rmnzmtrfs` 在 namespace `deveval-rmnzmtrfs` 一直处于 `ImagePullBackOff`,`kubectl describe` 提示无法从镜像仓库拉取镜像,请定位原因。

**评分理由**:

> Agent 完全遗漏了诊断私有仓库 ImagePullBackOff 的关键检查项：kubectl describe pod（查看事件）、imagePullSecrets 字段验证。Agent 在获取镜像名称和等待状态后直接跳转节点日志检查，被日志中的 'no such host' 误导信息欺骗（期望诊断明确指出的红色工单陷阱），得出错误的 DNS 根因结论，而不是实际的缺少镜像拉取凭据问题。

**期望结论**:

```
**根因**: Pod 使用的镜像 `registry.internal.example.com/private/no-such-app:v1.0.0` 来自私有仓库,但 `spec.imagePullSecrets` 为空,Pod 所在的 ServiceAccount 也未关联任何 docker-registry 类型的 Secret,kubelet 匿名拉取被镜像仓库拒绝,事件里会出现 `Failed to pull image ... unauthorized` 或 `ErrImagePull`,随后进入 `ImagePullBackOff`。红色工单中"节点网络抖动"是误导,此故障与节点网络无关。

**修复建议**:
1. 创建 `kubernetes.io/dockerconfigjson` 类型的 Secret,存放私有仓库凭据;
2. 在 Pod `spec.imagePullSecrets` 中引用该 Secret,或将其绑定到 Pod 使用的 ServiceAccount;
3. 顺带确认镜像 tag 存在于仓库,避免凭据正确但 tag 不存在的二次失败。

---
```

**Agent 实际结论**:

```
 我来帮你排查这个 ImagePullBackOff 问题。首先进行环境检查，然后使用专门的诊断技能来定位根因。 已了解环境和诊断流程。现在按照技能步骤进行排查：  获取到关键信息：
- **节点**: `cpu-10-208-55-85`
- **镜像**: `registry.internal.example.com/private/no-such-app:v1.0.0`
- **等待消息**: Back-off pulling image

这是一个私有镜像仓库。现在查看 containerd 日志获取具体错误原因： ## 诊断结论

**根因**: **DNS 解析失败** — 节点无法解析镜像仓库的域名 `registry.internal.example.com`

Containerd 日志显示的关键错误：
```
dial tcp: lookup registry.internal.example.com on 10.208.12.100:53: no such host
```

---

## 修复建议

请按以下优先级检查：

### 1. 检查域名拼写是否正确
- 确认镜像仓库地址 `registry.internal.example.com` 是否拼写正确
- 对比工作负载 YAML 中的 image 字段配置

### 2. 检查节点 DNS 配置
在节点 `cpu-10-208-55-85` 上执行：
```bash
# 检查 DNS 解析
nslookup registry.internal.example.com

# 检查 /etc/resolv.conf 的 nameserver 配置
cat /etc/resolv.conf

# 测试使用指定 DNS 服务器解析
nslookup registry.internal.example.com 10.208.12.100
```

### 3. 修复方案（选择其一）

| 方案 | 操作 |
|------|------|
| **A. 修正域名** | 如果域名拼写错误，修改 Pod 的 image 字段为正确地址 |
| **B. 添加 hosts 记录** | 在节点 `/etc/hosts` 中添加仓库 IP 映射（临时方案） |
| **C. 配置 DNS 解析** | 确保内部 DNS 服务器（10.208.12.100）能解析 `internal.example.com` 域，或在节点 resolv.conf 中添加能解析该域的 nameserver |
| **D. 改用 IP 访问** | 如果仓库有静态 IP，可将 image 改为 `http://<ip>/private/no-such-app:v1.0.0`（需配置 insecure-registries）|

---

**推荐操作**: 先检查域名拼写和内部 DNS 服务器配置，这是最可能的原因。
```

**Agent 执行的命令**:

```
[cluster_info] {}
[credential_list] {}
[read] {"path":"/home/yye/siclaw/.siclaw/skills/user/b1ba878783751e80/resolved/image-pull-debug/SKILL.md"}
[bash] kubectl get pod imagepull-private-002-rmnzmtrfs -n deveval-rmnzmtrfs -o jsonpath='{.spec.nodeName}'
[bash] kubectl get pod imagepull-private-002-rmnzmtrfs -n deveval-rmnzmtrfs -o jsonpath='{.status.containerStatuses[*].image}'
[bash] kubectl get pod imagepull-private-002-rmnzmtrfs -n deveval-rmnzmtrfs -o jsonpath='{.status.containerStatuses[0].state.waiting.message}'
[read] {"path":"/home/yye/siclaw/.siclaw/skills/user/b1ba878783751e80/resolved/node-logs/SKILL.md"}
[node_script] get-node-logs.sh
```

**注入输出**: `namespace/deveval-rmnzmtrfs unchanged
pod/imagepull-private-002-rmnzmtrfs created`

---

### ⏭️ `node-notready-003` — 节点 NotReady 导致 Pod 长期 Pending

**结果**: SKIP
**原因**: Fixture-replay (stubbed case) not yet implemented. stubReason: 节点级故障无法在共享测试集群安全模拟,线上排查时故障已由 kubelet 自愈,使用历史 fixture 回放

**期望结论**:

```
**根因**: 集群中 `node-3` 的 kubelet 已停止上报状态(`Kubelet stopped posting node status`),节点被控制面标记为 `NotReady`,并自动打上 `node.kubernetes.io/unreachable:NoSchedule/NoExecute` 污点;同时该业务 Pod 配置了 nodeAffinity/selector,只能调度到符合条件的节点上,其余 4 个 Ready 节点不匹配亲和性,因此 `default-scheduler` 报 `FailedScheduling`,Pod 长时间 Pending。真正的调度失败原因是**匹配节点 node-3 不可用**,而不是集群容量不足。

**修复建议**:
1. 优先排查 `node-3` kubelet 与 API Server 的连通性:检查 kubelet 进程、节点网络、`kubelet.service` 日志;
2. 短期缓解:放宽 Pod 的 nodeAffinity/selector,让它能调度到其他 Ready 节点;
3. 长期改进:对关键业务添加 `tolerations` 或扩展可调度节点池,避免单节点故障阻塞调度。
```
```

---

