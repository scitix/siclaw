# 回归测试报告 — 2026-04-15T06:44:41.420Z

**整体结果**: ✅ PASS

| 字段 | 值 |
|---|---|
| Run ID | `rmnzonexd` |
| 开始时间 | 2026-04-15T06:42:03.217Z |
| 结束时间 | 2026-04-15T06:44:41.420Z |
| 总耗时 | 2m38s |
| 总计 | 4 |
| 通过 | 3 |
| 失败 | 0 |
| 跳过 | 1 |
| 错误 | 0 |

## 汇总

| Case | 类型 | 命令分 | 结论分 | 阈值 | 耗时 | 结果 |
|---|---|---|---|---|---|---|
| `oom-basic-001` | reproducible | 4 | 5 | 4/4 | 49.8s | ✅ PASS |
| `probe-unreachable-002` | reproducible | 4 | 4 | 3/4 | 40.3s | ✅ PASS |
| `probe-timeout-003` | reproducible | 4 | 4 | 3/4 | 1m8s | ✅ PASS |
| `node-notready-003` | stubbed | - | - | 3/4 | 0ms | ⏭️ SKIP |

## 故障注入命令清单

> 以下命令由 Runner 在 agent 解题**之前**执行,agent 全程无法看到这些命令或 YAML。

### `oom-basic-001`

- Pod: `deveval-regress-oom-basic-rmnzonexd-20260415-144203`
- Namespace: `deveval-rmnzonexd`

```bash
kubectl apply -f - --validate=warn <<'EOF'
apiVersion: v1
kind: Pod
metadata:
  name: deveval-regress-oom-basic-rmnzonexd-20260415-144203
  namespace: deveval-rmnzonexd
  labels:
    deveval/case: oom-basic-001
spec:
  restartPolicy: Always
  containers:
  - name: app
    image: busybox:1.36
    command: ["sh", "-c", "while true; do dd if=/dev/zero of=/dev/null bs=100M; done"]
    resources:
      limits:
        memory: "50Mi"
      requests:
        memory: "20Mi"
EOF
```

**注入输出**: `namespace/deveval-rmnzonexd created
pod/deveval-regress-oom-basic-rmnzonexd-20260415-144203 created`

### `probe-unreachable-002`

- Pod: `deveval-regress-probe-unreachable-rmnzonexd-20260415-144253`
- Namespace: `deveval-rmnzonexd`

```bash
kubectl apply -f - --validate=warn <<'EOF'
apiVersion: v1
kind: Pod
metadata:
  name: deveval-regress-probe-unreachable-rmnzonexd-20260415-144253
  namespace: deveval-rmnzonexd
  labels:
    deveval/case: probe-unreachable-002
spec:
  restartPolicy: Always
  containers:
  - name: app
    image: busybox:1.36
    command: ["sh", "-c", "while true; do sleep 30; done"]
    livenessProbe:
      tcpSocket:
        host: 10.255.255.1
        port: 80
      initialDelaySeconds: 5
      periodSeconds: 5
      timeoutSeconds: 2
      failureThreshold: 2
EOF
```

**注入输出**: `namespace/deveval-rmnzonexd unchanged
pod/deveval-regress-probe-unreachable-rmnzonexd-20260415-144253 created`

### `probe-timeout-003`

- Pod: `deveval-regress-probe-timeout-rmnzonexd-20260415-144333`
- Namespace: `deveval-rmnzonexd`

```bash
kubectl apply -f - --validate=warn <<'EOF'
apiVersion: v1
kind: Pod
metadata:
  name: deveval-regress-probe-timeout-rmnzonexd-20260415-144333
  namespace: deveval-rmnzonexd
  labels:
    deveval/case: probe-timeout-003
spec:
  restartPolicy: Always
  containers:
  - name: app
    image: busybox:1.36
    command: ["sh", "-c", "while true; do sleep 30; done"]
    livenessProbe:
      exec:
        command: ["sh", "-c", "sleep 10 && echo ok"]
      initialDelaySeconds: 5
      periodSeconds: 5
      timeoutSeconds: 2
      failureThreshold: 2
EOF
```

**注入输出**: `namespace/deveval-rmnzonexd unchanged
pod/deveval-regress-probe-timeout-rmnzonexd-20260415-144333 created`

## 失败 / 跳过 / 错误 详情

### ⏭️ `node-notready-003` — 节点 NotReady 导致 Pod 长期 Pending

**结果**: SKIP
**原因**: Fixture-replay (stubbed case) not yet implemented. stubReason: 节点级故障无法在共享测试集群安全模拟,线上排查时故障已由 kubelet 自愈,使用历史 fixture 回放

**期望结论**:

```
**根因**: 集群中 `node-3` 的 kubelet 已停止上报状态(`Kubelet stopped posting node status`),节点被控制面标记为 `NotReady`,并自动打上 `node.kubernetes.io/unreachable:NoSchedule/NoExecute` 污点;同时该业务 Pod 配置了 nodeAffinity/selector,只能调度到符合条件的节点上,其余 4 个 Ready 节点不匹配亲和性,因此 `default-scheduler` 报 `FailedScheduling`,Pod 长时间 Pending。真正的调度失败原因是**匹配节点 node-3 不可用**,而不是集群容量不足。

**修复建议**:
1. 优先排查 `node-3` kubelet 与 API Server 的连通性:检查 kubelet 进程、节点网络、`kubelet.service` 日志;
2. 短期缓解:放宽 Pod 的 nodeAffinity/selector,让它能调度到其他 Ready 节点;
3. 长期改进:对关键业务添加 `tolerations` 或扩展可调度节点池,避免单节点故障阻塞调度。
```
```

---

